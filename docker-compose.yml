services:
  dev:
    build: .
    volumes:
      - .:/work
      - cargo-registry:/usr/local/cargo/registry
      - cargo-git:/usr/local/cargo/git
      - target:/work/target
    working_dir: /work

volumes:
  cargo-registry:
  cargo-git:
  target:
